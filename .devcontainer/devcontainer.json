{
  "name": ".minions",
  "customizations": {
    "vscode": {
      "extensions": [
        "joverina.rina-hermes-acp",
        "anthropic.claude-code"
      ]
    }
  },
  "postCreateCommand": "#commented off for manual install ./install.sh",
  "postStartCommand":  "bash $HOME/.minions/boot.sh >> /tmp/.minions.log 2>&1"
}